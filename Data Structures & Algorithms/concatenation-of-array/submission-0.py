class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        
        return nums + nums
        # n = len(nums)
        # capacity = n * 2

        # ans = [0] * capacity


        # for i in range(n):
        #     ans[i]=nums[i]
        
        # return ans